@page "/Samples/Komponentenhost_Misc"
@* @using ITVisions.Blazor.Controls *@

<h3>Komponentenhost_Misc</h3>
<p>
 Diese Razor Component <b>@this.GetType().FullName</b> kommt aus der DLL: <b><text>@System.Reflection.Assembly.GetExecutingAssembly().GetName().Name</text>.dll</b>!
</p>
@*<CounterPanel StartValue="10" ValueHasChanged="NewValueArrived" />*@

<CounterPanel ValueClass="@ValueClass" 
              ValueStruct="@ValueStruct" 
              ValueRecord="@ValueRecord"/>


<button @onclick="Refresh" class="btn btn-success">Refresh</button>
<p>
 @@ValueClass.Counter: @ValueClass.Counter
</p>
<p>
 @@ValueStruct.Counter: @ValueStruct.Counter
</p>
<p>
 @@ValueRecord.Counter: @ValueRecord.Counter
</p>

@code
{
 public CounterDataClass ValueClass { get; set; } = new() { Counter = 10 };
 public CounterDataStruct ValueStruct { get; set; } = new() { Counter = 10 };
 public CounterDataRecord ValueRecord { get; set; } = new() { Counter = 10 };

 void Refresh()
 {

 }

}

<hr />

<p>Dieser Inhalt ist nur sichtbar, wenn das Environment "Development" ist<br />
 <ITVisions.Blazor.Controls.Debug>Dieser <a href="https://www.IT-Visions.de">Link</a> sollte nur zur Entwicklungszeit sichtbar sein!</ITVisions.Blazor.Controls.Debug>
</p>

@* <p>Hier fehlt der Namensraum: Debug wird als HTML-Tag interpretiert und der Inhalt ist immer sichtbar!<br />

 <Debug>
  Dieser <a href="https://www.IT-Visions.de">Link</a> sollte nur zur Entwicklungszeit sichtbar sein, aber leider ist er auch im produktiven Betrieb sichtbar
 </Debug>
 *@
</p>